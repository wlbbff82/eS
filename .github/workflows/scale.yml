name: worker

on:
  push:

jobs:
  ability:
    runs-on: ubuntu-latest
    strategy:
      matrix:
        politics: ["technician", "nuisance", "presently", "issue", "minor"]
        inward: ["secure", "rank", "distant", "symbolize"]
    steps:
    - uses: actions/checkout@v6
    - uses: actions/setup-java@v5
      with:
        distribution: 'zulu'
        java-version: '25'
    - run: java -jar Om
